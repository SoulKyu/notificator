apiVersion: v1
kind: Service
metadata:
  name: notificator-alertmanager
  labels:
    app: notificator-alertmanager
spec:
  type: ClusterIP
  ports:
  - port: 9093
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: notificator-alertmanager